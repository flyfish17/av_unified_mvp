# 任务单 · 视听底座线 R1 — 回流落地与完善（2026-08-20 立项）

> 于总拍板开线。执行模型：**Mac 开发 → GitHub 事实源 → 62 部署验证**（5.6 保持纪要机不动，等 7 系列正确主机后回归研发机）。终端认领执行，监理（Obsidian 侧）独立复验。
> 背景盘点见 `docs/回流-纪要机线沉淀-20260820.md`；纪要分支代码已全在主干，本批核心 = 让部署点追上主干 + 两个有具名客户的完善项。

## R1a · 固化 62 部署脚本 + 刷新 62 到 HEAD 🔴（先行）

- **产出 `scripts/deploy-62.sh`**：Mac 端一键 —— `git archive HEAD` 打 tar（排除密钥/日志，参照湖森部署方案卡 §2 的干净打包）→ `sshpass -p xc ssh proembed@192.168.5.62` 推送解开 → **排除/保留板上 `config/system_config.yaml`、`data/`、`summaries/`、venv**（板上实际配置与数据绝不覆盖）→ `systemctl restart av-demo` → 冒烟（:5050=200 + 12 模块进程数 + door_access 在 + node-red 200）。
- 首次执行即刷新 62：板上代码停在 8/18 tar 版，缺 8/19-20 全部改动（物理 ID 身份、/api/download、copyText、profile 显隐、husion quiesce、纪要 md 命名等）。
- ⚠️ **红线：62 是湖森演示机，动之前跟于总约窗口**；刷前备份板上整个 `~/av_unified_mvp`（mv 加日期后缀）可整体回退。
- 注意：62 板上 `main.py` 是旧基线+door_access 旧式注册（8/19 监理定性等价非分叉），HEAD 覆盖后走新式 profile 注册——冒烟必须确认 door_access 进程在、门口弹窗链路活。
- 62 的 config 无 `app_profile` 行 = full 默认，行为不变；HEAD 新增的 `record_session` 默认开——62 是 mic 形态（audio_processor），net_audio_capture 不起，recorder 不会启动，零影响。

## R1b · creator_cc flows 快照回流（Mac 纯文件操作）

- 从 `/Users/yumacs/Developer/creator_cc/flows/cc-merged-20260820.json` 复制到本仓 `node-red/snapshots/cc-merged-20260820.json`，文件头部（或旁置 README）注明：**事实源 = creator_cc 仓（flyfish17/creator_cc），本文件为只读快照，不在此编辑**（单一事实源铁律）。
- 同时收录 `flows/backup-62-1880-flows-20260819.json`（62 视听侧 flows 备份）到同目录，同样标注来源与日期。
- commit + push。不做 flows 内容开发——中控协议链的演进在 cc 仓/109 线。

## R1c · 公司演示环境 P0 · 转写大屏页（具名客户 = 第 0 号客户）

- **产出 `/bigscreen` 路由 + 大屏转写页**：全屏深色、大字号（3-5 米可读）、发言人分色分段（复用物理 ID 身份与当前命名表）、自动跟随最新、无任何操作按钮/导航（纯展示面）。顶部一行小字：时间 + "CREATOR AI 实时转写"品牌位。
- 数据源复用现有 SSE 转写通道（web/server.py 已有），**不加新后端逻辑**；纯前端新页面 + server 一个 render 路由。
- 场景：来访/例会时研发中心百寸电视（HDMI/HDC910 链路，见演示环境总览 §五 P0）全屏打开 `http://<板>:5050/bigscreen`。
- 词表：演示前把公司人名灌 FunASR hotwords（板上 `/workspace/FunASR/.../hotwords.txt` 已有机制），任务内把"怎么灌词表"写成 3 行操作注记入 docs。
- 验收：62 上真人说话 → 大屏页 3 米外可读、分段分色正确、改名后大屏同步（按当前命名表渲染，遵守回流清单 C-2 原则）。

## 边界（本批不做）

- ❌ 运维摘要卡（湖森红线"不接运维"；渤船/演示环境有具名需求再开）
- ❌ Jetson/av_scene 归位（挂 net-6000 立项）
- ❌ HTTPS 化（内网自签麻烦>收益，下载"保留"确认接受为平台行为）
- ❌ node-red flows 内容开发（归 creator_cc 线）
- ❌ 动 5.6（纪要机客户体验期 + 7 系列唯一实机）

## 通用红线

- 每次 commit 后 push（GitHub = 事实源，总部随时拉取）
- ssh 远程 pkill/pgrep pattern 用 `[x]` 字符类打断；重启等 `pgrep=0` 再启（LESSONS 速查表 8/20 新条）
- 板上 config/data/summaries 永不覆盖；改动全部走 CHECK_LOG 式完工自述，监理复验后放行
- 62 演示优先：任何窗口内动作前确认"演示不在进行"

## 验收线（监理）

- R1a：deploy-62.sh 幂等可重跑；刷后 62 十二模块全在 + door_access 活 + demo 动线（`:5050` 演示按钮 + 语音闭环）不回归；板上文件 md5 抽 3 个 = GitHub HEAD
- R1b：快照文件 + 来源标注入仓
- R1c：大屏页用户亲验（3 米可读性由于总现场判）
