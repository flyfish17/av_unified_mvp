# av_unified_mvp

## 项目定位
**长期技术底座**，不是一次性 MVP。本仓库的模块、接口约定、启动脚本会被复用到后续多个项目；任何决策优先考虑"是否可累积、可复用"，其次才是"本次最快"。

## 架构原则：解耦的 MQTT 订阅
- 模块之间**不直接调用**，通过 MQTT（见 `core/mqtt_bridge.py`）发布/订阅事件通信。
- 新功能优先做成独立子模块（参考 `modules/` 现有结构），不要塞进既有模块的内部逻辑。
- 不要让模块 A 直接 `import` 模块 B 的内部实现，只通过消息交互。

## 节奏
1. 先跑通整体框架（视频 / 音频 / LLM / MQTT / Dashboard 串起来）。
2. 再逐步深化各子模块。
3. 子模块成熟即可单独抽出做小场景应用。

不要为了某个子模块的完美度，阻塞整体框架的可运行性。

## 开发计划与进度
- 开发计划维护在 `DEVELOPMENT_PLAN.md`（仓库根）。
- 进度同步追加到该文件末尾"## 进度日志"小节，每条含：日期、本次推进、未完成项、下次接手所需上下文。
